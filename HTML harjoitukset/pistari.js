function jarjesta() {
    var Luku;
    Luku = parseInt(document.getElementById('eka').value);
   
    document.write("Annoit luvun: " >= 0 );

    if (Luku >= 0 ) {
        k
        if (Luku < 0 ) {
            document.write(" Luku on negatiivinen ");
        }
        else (Luku > 0 ) {
            document.write(" Luku on positiivinen ");
        }
    }
}